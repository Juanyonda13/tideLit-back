# Configuración de producción para TideLit Backend
APP_ENV=prod
APP_SECRET=your-secret-key-change-in-production-please-use-a-strong-random-string

# Base de datos MySQL
DB_USER=laravel
DB_PASSWORD=LaravelROOT2024!
DB_HOST=database
DB_PORT=3306
DB_NAME=tideLit_db
DATABASE_URL="mysql://%env(DB_USER)%:%env(DB_PASSWORD)%@%env(DB_HOST)%:%env(DB_PORT)%/%env(DB_NAME)%?serverVersion=8.0.32&charset=utf8mb4"

# Configuración adicional
MAILER_DSN=null://null
